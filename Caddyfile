# Replace chores.home with your actual domain.
# Caddy auto-provisions TLS via Let's Encrypt.
chores.home {
  reverse_proxy app:3000

  # Serve uploaded photos directly from the app container
  # (already handled by Next.js static serving in /public/uploads)

  encode zstd gzip
}
