# Replace chores.home with your actual domain.
# Caddy auto-provisions TLS via Let's Encrypt.
# For production, replace :80 with your domain (e.g. chores.home) and
# Caddy will auto-provision TLS via Let's Encrypt.
:80 {
  reverse_proxy app:3000
  encode zstd gzip
}
